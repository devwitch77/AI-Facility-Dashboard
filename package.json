{
  "name": "ai-facility-root",
  "private": true,
  "scripts": {
    "build": "cd dashboard/client && npm install && npm run build",
    "start": "cd dashboard/client && npm run preview -- --host 0.0.0.0 --port=${PORT:-4173}"
  }
}
